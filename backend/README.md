# CallScope Backend

从本目录执行 `uv sync --frozen`、`uv run alembic upgrade head`、`uv run uvicorn app.main:app --host 127.0.0.1 --reload`。

配置模板见 [.env.example](.env.example)，完整运行与验证方式见[根 README](../README.md)。
